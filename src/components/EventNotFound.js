import React from "react";

import "./EventNotFound.css";

const EventNotFound = (props) => {
    return (
        <div className="twelve wide column column-right eventNotFound">
            You don't have todo list. If you have please add your todo event.
        </div>
    );
};

export default EventNotFound;